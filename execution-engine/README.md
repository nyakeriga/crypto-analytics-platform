# Execution Engine

Handles order routing, exchange connectivity, and risk management for live and simulated trading.

## Example Order Router (TypeScript)
```ts
export function routeOrder(order) {
  // Validate and send order to exchange connector
}
```
